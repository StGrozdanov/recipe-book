import { StyleSheet } from "react-native";

export const chartStyles = StyleSheet.create({
    container: {
        marginStart: -10
    },

    title: {
        textAlign: "center",
        marginVertical: 25,
    },
});